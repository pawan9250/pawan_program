#include<conio.h>
#include<stdio.h>

void main()
{

  clrscr();
  
  int arr[50], size, insert, i, pos;
  
  cout<<"enter array size:";
  cin>>size;
  cout>>"enter aaray element :";
  
  for(i=0; i<size; i++)
  {
     cin>>arr[i];
  }
  cout<<"enter element to be intsert";
  cin>>insert;
  cout<<"at which position (enter index number)?";
  cin>>pos;
  
  for(i=size; i>=pos; i==)
  {
       arr[i]=arr[i-1];
  }
  arr[pos]=insert;
  cout<<"element insert succesfully\n";
  cout<<"now new array is :\n";
  
  for(i=0; i=size; i++)
  {
      cout<<arr[i]<<" ";
  }
  getch();
  }
